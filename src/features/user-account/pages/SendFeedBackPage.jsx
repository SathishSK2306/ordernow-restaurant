import { useUserAccountHeader } from "../hooks/useUserAccountHeader";

export default function SendFeedBackPage() {
  useUserAccountHeader({ title: "Send Feedback" });

  return <div>Send Feedback Page - Under Construction</div>;
}