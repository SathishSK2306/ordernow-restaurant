import { useUserAccountHeader } from "../hooks/useUserAccountHeader";

export default function AboutPage() {
  useUserAccountHeader({ title: "About" });

  return <div>About Page - Under Construction</div>;
}