import { useUserAccountHeader } from '../hooks/useUserAccountHeader';

export default function UserReviewsPage() {
  useUserAccountHeader({ title: 'Your Reviews' });
  return <div>User Reviews Page - Under Construction</div>;
}