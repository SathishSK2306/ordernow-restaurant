import { useUserAccountHeader } from "../hooks/useUserAccountHeader";

export default function UserSettingsPage() {
  useUserAccountHeader({ title: "Settings" });

  return <div>User Settings Page - Under Construction</div>;
}