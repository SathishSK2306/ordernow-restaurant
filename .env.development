# Environment: DEVELOPMENT

# Gotcha:
# 1) Only variables prefixed with VITE_ are exposed to your React app when using Vite. Others remain private to the build process.

# API base URL for development
VITE_API_BASE_URL=http://localhost:8000/api